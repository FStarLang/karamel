type exit_code =
  | EXIT_SUCCESS
  | EXIT_FAILURE

let exit = exit

let string_of_literal s = s
let print_string = print_string

let fflush = flush
let stdout = stdout
let stderr = stderr

let _zero_for_deref = FStar_UInt32.zero

